/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    experimental: {
    appDir: true, // Ensures the app folder is properly handled
  },
};

export default nextConfig;
